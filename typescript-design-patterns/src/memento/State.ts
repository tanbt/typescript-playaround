export interface State {
  /**
   * @param _content
   *          the current content
   * @param _cursorX
   *          the current x position of cursor
   * @param _cursorY
   *          the current y position of cursor
   */
  content: string;
  cursorX: number;
  cursorY: number
}